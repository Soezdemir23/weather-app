import { Typography } from "@material-tailwind/react";

export default function Header() {
  // we need to get the days

  return (
    <header className="bg-blue-gray-200 p-4">
      <Typography variant="h1">Weatherlicious</Typography>
    </header>
  );
}
